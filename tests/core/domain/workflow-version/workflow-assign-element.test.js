import WorkflowAssignment from '~/core/domain/workflow-version/elements/assign/workflow-assignment';

describe('Workflow Assign Element', () => {
    describe('Assignment', () => {
        describe('When assignment operator is set', () => {
            it('Should assign', () => {
                
            });
        });
    });
});