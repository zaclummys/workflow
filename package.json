{
  "name": "workflow",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/nextjs"
  ],
  "engines": {
    "node": ">=18.17.0",
    "pnpm": "^8.15.0"
  },
  "scripts": {
    "dev": "pnpm --filter @workflow/nextjs dev",
    "test": "vitest",
    "storybook": "pnpm --filter @workflow/nextjs storybook"
  },
  "packageManager": "pnpm@8.15.0",
  "devDependencies": {
    "vitest": "^1.5.2"
  },
  "dependencies": {
    "argon2": "^0.40.1"
  }
}